package CollisionHandling;

/**
 * 3 possible outcomes of collision detection: Incompatible types, collided and not collided
 */
public enum CollisionType {
    NOT_COMPATIBLE,
    COLLIDED,
    NOT_COLLIDED
}
